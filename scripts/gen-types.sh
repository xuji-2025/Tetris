#!/usr/bin/env bash
set -e

echo "🔧 Generating protocol types from JSON schema..."

# TODO: Implement type generation when needed
# For now, we'll hand-write the types in Python and TypeScript

echo "⚠️  Type generation not implemented yet - manually sync types with proto/schema/v1.json"
